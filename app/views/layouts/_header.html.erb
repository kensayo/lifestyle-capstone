<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom border-3 border-warning">
  <div class="container-fluid">
    <%= link_to root_path, class: 'navbar-brand' do %>
      <%= image_tag 'sweet-world.png', style: 'width:225px' %>
    <% end %>
    <div class="d-flex justify-content-between w-100" id="navbarText">
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% @categories.each do |category| %>
          <li class="nav-item">
            <%= link_to category.name, category_path(category), class: "nav-link fw-bold" %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if !user_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Sign In', user_session_path, class: 'nav-link fw-bold' %>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold">|</a>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign Up', new_user_registration_url, class: 'nav-link fw-bold' %>
        </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'New', new_recipe_path, class: "nav-link fw-bold" %>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-bold">|</a>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete', class: 'nav-link fw-bold' %>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-bold">|</a>
          </li>
          <li class="nav-item">
            <%= link_to truncate(current_user.email, separator: "@"), "#", class: 'nav-link fw-bold' %>
          </li>
        <% end %>
        </ul>
    </div>
  </div>
</nav>